REGLA PERMANENTE DE SEGURIDAD:
Nunca elimines, sobreescribas ni 'limpies' el archivo .env real del
usuario, ni la carpeta logs/, ni archivos de estado local que ya
existieran antes de tu tarea actual. Si necesitas generar estos archivos
para probar algo, créalos en una carpeta separada (test-tmp/) y bórralos
solo de ahí. Si detectas que alguno de estos archivos del usuario fue
modificado o borrado por error, repórtalo explícitamente en tu resumen
final.
